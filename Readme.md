Django based webapp for predicting mileage of automobile. Uses Decision regressor. Trained on AUTOMPG dataset(kaggle).

SET UP INSTRUCTIONS :
1) Clone this repo
2) pip install req.txt
3) python manage.py runserver
